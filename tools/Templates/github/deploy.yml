# ====================================================================
# ⚠️  DEPRECATED — DO NOT USE  ⚠️
# ====================================================================
# This file is kept ONLY as a reference for what a STANDALONE deploy
# pipeline would look like. It is NOT used by the Hermes Factory.
#
# REASON: Child repositories MUST NOT deploy themselves.
# Deployment is exclusively orchestrated by the Control Plane
# (HERMES-ENTERPRISE/.github/workflows/deploy-child.yml).
#
# Key violations in this template (if used for child repos):
#   1. id-token: write  →  Child must NOT have OIDC deployment privileges
#   2. azure/login@v2   →  Child must NOT have Azure login capability
#   3. Azure/webapps-deploy@v3  →  Deployment via Control Plane only
#   4. Creates per-project ASP  →  Must use shared ASP-IAUR
#   5. Gunicorn startup cmd  →  Must use startup.sh
#
# The factory (Crear-HermesProyecto.ps1) intentionally generates ONLY
# ci.yml for child repos. See line 95 of the factory script:
#   "# Note: No deploy.yml for child repos. Deployment orchestrated by Control Plane."
#
# If you need to deploy a Hermes child, trigger the Control Plane workflow:
#   .github/workflows/deploy-child.yml  (in HERMES-ENTERPRISE repo)
# ====================================================================
