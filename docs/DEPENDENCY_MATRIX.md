# Dependency Matrix

WorkspaceService -> ConfigurationService -> LoggingService -> GitService -> ProvisionService -> PipelineService -> BootstrapService

(Se detallará por componente cuando se migre cada servicio)
